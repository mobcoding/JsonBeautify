# 隐私政策（Privacy Policy）

生效日期：2026-08-14

## 概述

JSON Beautify（以下简称「本扩展」）是一款运行在本机浏览器内的开发者工具扩展。本扩展的设计目标是在**完全不联网、不上传任何数据**的前提下，为用户提供 JSON 文本的格式化、美化、压缩、验证、转义和差异比较功能。

请在安装或使用本扩展前仔细阅读本隐私政策。

## 一、我们收集或上传的数据：无

本扩展**不会收集、上传、存储或传输**任何用户数据到第三方服务器或开发者服务器。所有功能均在用户本机浏览器内完成。

### 不收集的数据范围（包括但不限于）

- 用户输入或粘贴到扩展输入框中的 JSON 或任意文本；
- 用户当前浏览的页面 URL、标题或正文内容（仅在用户主动触发，例如「读取页面 JSON」「美化当前页面 JSON」等右键或按钮操作时，会在当前浏览器标签页的内存中读取；处理过程中不会离开本机）；
- 扩展自身的设置项（仅保存在 `chrome.storage.local` 中，由用户浏览器管理，不会同步到开发者服务器）。

## 二、本扩展使用的权限（Permissions）说明

为实现上述纯本地化功能，本扩展在 `manifest.json` 中声明了以下权限，我们逐一说明其用途：

| 权限 | 用途 | 触发方式 |
| --- | --- | --- |
| `storage` | 将用户的偏好设置（例如默认缩进、是否默认按键名排序等）保存在本机浏览器的 `chrome.storage.local` 中，**不上传、不同步**到任何服务器。 | 自动触发，仅读写本机存储。 |
| `contextMenus` | 在网页右键菜单中添加「格式化选中的 JSON」「美化当前页面 JSON」「压缩选中的 JSON」「验证选中的 JSON」「复制美化后的 JSON」等条目。 | 用户在网页上右键点击时由浏览器渲染。 |
| `activeTab` | 当用户通过右键菜单或「读取页面」按钮操作时，临时获得当前活动标签页的访问权，用于读取文本内容或替换选区内的 JSON。 | 用户主动发起操作时才会生效；不会在后台静默访问任意页面。 |
| `scripting` | 在用户主动触发的场景下，在当前标签页中执行一次性脚本（例：读取选区文本、把格式化结果写回可编辑元素、复制到剪贴板）。 | 由用户点击右键菜单或面板按钮触发。 |
| `host_permissions: <all_urls>` | 由于用户可能在任意网站的页面或文本框中编辑 JSON（例如本地 `http://localhost`、私有 API、内网页面），需要允许右键菜单与读取页面按钮在任意站点生效。**扩展不会主动向这些站点发起网络请求。** | 用户在某个站点主动触发功能时生效。 |

## 三、Content Script（内容脚本）说明

本扩展在所有 `http` / `https` 页面中注入了一个轻量的 content script（`src/content.js`），其作用仅限于：

1. **监听来自扩展后台的消息**：仅在用户点击右键菜单时响应，执行与格式化相关的动作（例如替换选区、弹出自带 Toast 提示）。
2. **检测原始 JSON 页面**：当访问的 URL 以 `.json` 结尾、`Content-Type` 为 `application/json`，或整页仅有 `<pre>` 内容时，在用户启用「自动检测页面 JSON 响应」设置（默认开启）的情况下，自动美化页面并提供下载/复制等入口。

content script 在**不触发上述情况**时，不会读取页面内容或发起任何网络请求。

## 四、网络请求：无

本扩展**不发起任何主动的外部网络请求**。所有 JSON 解析、格式化、比较、验证、转义反转义等均通过浏览器内置的 `JSON.parse` / `JSON.stringify` 和本扩展自带的纯函数实现。

- 没有分析脚本；
- 没有远程日志；
- 没有第三方 SDK；
- 没有广告 SDK；
- 没有追踪脚本；
- 没有遥测（telemetry）。

## 五、本地存储与清除方式

本扩展仅使用 `chrome.storage.local` 保存用户偏好设置，数据内容为以下字段：

```json
{
  "indentSize": 2,
  "useTabIndent": false,
  "sortKeys": false,
  "autoFormatOnView": true,
  "syntaxHighlight": true
}
```

用户可随时在 Edge 中通过以下路径清除扩展的本机数据：

1. 打开 `edge://extensions`（管理扩展）；
2. 找到「JSON Beautify」条目；
3. 点击「详细信息」→「扩展选项」打开设置页；
4. 点击页面底部「清除本机数据」按钮。

或：直接卸载扩展，浏览器会一并清除与其相关的本地存储。

## 六、儿童隐私

本扩展面向普通开发者与一般用户设计，不具有吸引 13 岁以下儿童的内容，也不会故意收集任何儿童相关信息。

## 七、政策变更

如果将来扩展新增了需要上传数据的功能（例如在线 API 格式转换），我们会先：
1. 更新本隐私政策，并在扩展说明中明确提示；
2. 让用户**主动选择**是否开启该功能；
3. 默认保持关闭状态。

任何情况下，默认安装后的扩展行为都将保持「零上传、零遥测」的状态。

## 八、联系方式

如对本政策有疑问，可通过 Microsoft Edge Add-ons 扩展详情页的开发者邮箱，或项目仓库的 Issue 与我们联系。
